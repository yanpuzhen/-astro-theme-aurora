---
import BaseLayout from '../../layouts/BaseLayout.astro'
import SearchIsland from '../../islands/SearchIsland.vue'
import { messagesFor } from '../../lib/i18n'
import { localizedSitePath } from '../../lib/routing'
const locale = 'en' as const
const messages = messagesFor(locale)
const localeLinks = { en: localizedSitePath('/search/', 'en'), 'zh-CN': localizedSitePath('/search/', 'zh-CN') }
---
<BaseLayout title={`${messages.search} · Aurora`} canonicalPath="/search/" lang={locale} localeLinks={localeLinks} alternateLocaleLinks={localeLinks}><section class="search-page"><h1 class="page-heading">{messages.search}</h1><SearchIsland client:idle base={import.meta.env.BASE_URL} locale={locale} messages={messages} /></section></BaseLayout>
